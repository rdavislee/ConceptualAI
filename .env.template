# ==============================================================================
# ConceptualAI Environment Variables
# Copy this file to .env and fill in your values:  cp .env.template .env
# ==============================================================================

# --- Database (Required) ---
# For MongoDB Atlas, use mongodb+srv://. Encode special chars in password (e.g. % -> %25).
MONGODB_URL=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?appName=Cluster0
DB_NAME=headlessconceptual

# --- Authentication (Required) ---
JWT_SECRET=your-jwt-secret-at-least-32-chars-long

# --- Credential Vault / GitHub Linking ---
# Separate from JWT_SECRET. Used for vault-related server-side signing and
# other cryptographic operations for stored credentials and GitHub link state.
CREDENTIAL_VAULT_ENCRYPTION_KEY=your-credential-vault-secret-at-least-32-chars-long
# GitHub App OAuth settings for user linking and export.
GITHUB_APP_CLIENT_ID=
GITHUB_APP_CLIENT_SECRET=
# Public callback URL registered with the GitHub App. Include the /api/auth/github/callback path.
GITHUB_APP_CALLBACK_URL=

# --- Server ---
PORT=8000
REQUESTING_BASE_URL=/api
REQUESTING_ALLOWED_DOMAIN=*
REQUESTING_SAVE_RESPONSES=true

# --- AI Runtime ---
# Generated concepts resolve provider/model from these env vars.
# Current internal concept flows should use Gemini synchronously, with Gemini Flash as the default model.
AI_PROVIDER=gemini
AI_MODEL=gemini-flash-latest
# Generated implementation/sync validation runs use these explicit test envs.
GENERATED_TEST_AI_PROVIDER=gemini
GENERATED_TEST_AI_MODEL=gemini-flash-latest
GENERATED_AI_CALL_TIMEOUT_MS=45000
GENERATED_CONCEPT_TEST_TIMEOUT_MS=60000
GENERATED_SYNC_TEST_TIMEOUT_MS=30000
GENERATED_SYNC_TEST_AI_TOUCHING_TIMEOUT_MS=120000
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
XAI_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=

# --- Gemini AI (Required for pipeline) ---
GEMINI_MODEL=gemini-3-pro-preview
GEMINI_MODEL_FLASH=gemini-flash-latest
GEMINI_MODEL_PRO=gemini-3-pro-preview
GEMINI_CONFIG=./geminiConfig.json
# GEMINI_MODEL_PRO=gemini-1.5-pro
# GEMINI_API_BASE=https://generativelanguage.googleapis.com/v1beta
# GEMINI_TIER_CHECK_MODEL=gemini-1.5-pro
# GEMINI_VERIFY_TIMEOUT_MS=8000
# GEMINI_VERIFY_CACHE_TTL_MS=300000
# GEMINI_VERIFY_FAILURE_CACHE_TTL_MS=60000

# --- Sandboxing ---
# Maximum number of concurrent sandbox containers allowed.
# When this limit is reached, new pipeline requests are rejected with a capacity error.
# Tune based on available RAM and CPU. See sandboxing-plan.md for sizing guidance.
MAX_CONCURRENT_SANDBOXES=20

# --- Concept Library ---
# URL of the concept-library server used by design and implementation retrieval.
# HEADLESS_URL=http://localhost:8000/api

# --- Frontend Generation ---
# If frontend generation later needs a browserless service URL, it should use its own dedicated env var.

# --- Hosted Previewing ---
# Feature flag for manual build previews.
PREVIEWS_ENABLED=true
# Provider strategy. Supported: freestyle (default), deno, mock (tests).
PREVIEW_PROVIDER=freestyle
# Server-managed token used by preview deployments.
DENO_DEPLOY_TOKEN=
# Optional organization identifier for Deno Deploy app creation.
# DENO_DEPLOY_ORG_ID=
# Preview lifetime in minutes.
PREVIEW_TTL_MINUTES=15
# Mongo URL used for managed preview app databases.
# Defaults to MONGODB_URL when unset.
PREVIEW_MONGODB_URL=
# Database prefix for fresh preview databases.
PREVIEW_DB_PREFIX=preview
# Maximum concurrently active previews per owner.
PREVIEW_MAX_ACTIVE_PER_USER=1
# Hard timeout for a single preview launch attempt (milliseconds).
# PREVIEW_LAUNCH_TIMEOUT_MS=1200000

# --- Deno Provider Settings ---
# Server-managed token used by preview deployments.
DENO_DEPLOY_TOKEN=
# Optional organization identifier for Deno Deploy app creation.
# DENO_DEPLOY_ORG_ID=

# --- Freestyle Provider Settings ---
# Freestyle.sh API config for creating Preview Sandbox VMs
FREESTYLE_API_KEY=

# Optional timeout and outbound host allowlist for sandboxed preview builds.
# PREVIEW_SANDBOX_TIMEOUT=30m
# PREVIEW_SANDBOX_ALLOW_NET=registry.npmjs.org,jsr.io,deno.land,esm.sh,api.deno.com,dl.deno.land

# --- Alternative AI Providers (Optional) ---
# AI_PROVIDER=gemini
# AI_MODEL=
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# XAI_API_KEY=
# GOOGLE_GENERATIVE_AI_API_KEY=
